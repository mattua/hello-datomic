.
├── docker-compose.yml
├── datomic
│   ├── config
│   │   └── transactor.properties
│   └── data
└── app
├── Dockerfile
├── pom.xml
└── src
└── main
└── java
└── com
└── example
└── DatomicDemo.java

# run the database
docker-compose up datomic